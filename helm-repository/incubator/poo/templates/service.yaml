apiVersion: v1
kind: Service
metadata:
  name: {{ include "poo.fullname" . }}
  labels:
    {{- include "poo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "poo.selectorLabels" . | nindent 4 }}
