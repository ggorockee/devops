apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poo.fullname" . }}-test-connection"
  labels:
    {{- include "poo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
