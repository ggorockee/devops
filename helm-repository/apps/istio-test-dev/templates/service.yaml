apiVersion: v1
kind: Service
metadata:
  name: {{ include "istio-test-dev.fullname" . }}
  labels:
    {{- include "istio-test-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "istio-test-dev.selectorLabels" . | nindent 4 }}
