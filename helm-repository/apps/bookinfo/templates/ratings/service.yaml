{{ $fullname := include "bookinfo.fullname" . }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-ratings" $fullname}}
  labels:
    {{- include "bookinfo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ratings.service.type }}
  ports:
    {{- toYaml .Values.ratings.service.ports | nindent 4 }}
  selector:
    {{- toYaml .Values.ratings.podLabels | nindent 4 }}
