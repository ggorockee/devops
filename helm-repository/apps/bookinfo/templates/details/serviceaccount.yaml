{{ $name := include "bookinfo.name" . }}
{{- if .Values.details.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ printf "%s-details" $name}}
  labels:
    {{- with .Values.details.podLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.details.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.details.serviceAccount.automount }}
{{- end }}
