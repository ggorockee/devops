apiVersion: v1
kind: Service
metadata:
  name: {{ include "airbnb-server.fullname" . }}
  labels:
    {{- include "airbnb-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "airbnb-server.selectorLabels" . | nindent 4 }}
